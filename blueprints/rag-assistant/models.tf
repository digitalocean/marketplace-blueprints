# Model UUIDs are resolved by the do-terraform service from internal names
# via the GenAI models API before calling terraform.
# No data source lookup needed — UUIDs are passed directly as variables.
